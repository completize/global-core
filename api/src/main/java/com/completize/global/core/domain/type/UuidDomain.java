package com.completize.global.core.domain.type;

import com.completize.global.core.domain.Domain;

import java.util.UUID;

/**
 * A domain for {@link UUID} values.
 *
 * @author wgs
 * @version 1.0
 * @since 1.0
 */
public interface UuidDomain extends Domain<UUID> {
}
